version: '3.7'

services:
  server:
    image: nginx:latest
    container_name: invoiceninja-nginx
    restart: unless-stopped
    env_file: stack.env
    volumes:
      - ${IN5VOL}/config/nginx/in-vhost.conf:/etc/nginx/conf.d/in-vhost.conf:ro #must modify this file, replace app with invoiceninja-app
      - ${IN5VOL}/docker/app/public:/var/www/app/public:ro
    depends_on:
      - app
    #ports:
      #- "8180:80"
      #- "8443:443"

  app:
    image: invoiceninja/invoiceninja:5.6.5
    container_name: invoiceninja-app
    env_file: stack.env
    restart: unless-stopped
    volumes:
      - ${IN5VOL}/docker/app/public:/var/www/app/public:rw,delegated
      - ${IN5VOL}/docker/app/storage:/var/www/app/storage:rw,delegated
    depends_on:
      - db

# Use LISO mariaDB
  db:
    image: lscr.io/linuxserver/mariadb:latest
    container_name: invoiceninja-db
    restart: unless-stopped
    env_file: stack.env
    volumes:
      - ${IN5VOL}/docker/db:/config

networks:
  default:
    name: ${CONNET}
    external: true